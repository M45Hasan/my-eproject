import React from 'react'

const Button = ({butName}) => {
  return (
    <button className='bg-primary text-[white] font-dm font-bold text-sm py-4 px-[85px] '>{butName}</button>
  )
}

export default Button